(() => {

	function elById(id) {
		return document.getElementById(id);
	}

	const usernameText = elById('username');
	const paswordText = elById('password');
	const loginBtn = elById('login_btn');



});
